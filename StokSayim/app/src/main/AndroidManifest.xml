<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.pethoalpar.zxingexample"
          xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/barkod"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".kontrollusay">
            <intent-filter>
                <action android:name="android.intent.action.kontrollusay"/>

                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity android:name=".Hakkimizda">
            <intent-filter>
                <action android:name="android.intent.action.kontrollusay"/>

                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <activity android:name=".kontrolsuzsay">
            <intent-filter>
                <action android:name="android.intent.action.kontrolsuzsay"/>

                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity android:name=".liste_goster">
            <intent-filter>
                <action android:name="android.intent.action.kontrolsuzsay"/>

                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity android:name=".fileexplorer">
            <intent-filter>
                <action android:name="android.intent.action.kontrolsuzsay"/>

                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

    </application>

</manifest>
